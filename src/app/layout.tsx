import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const jetbrainsMono = JetBrains_Mono({
  variable: "--font-jetbrains-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Healing & Restoration in Scripture | Bible Verses & Stories for Forgiveness, Peace & Reconciliation",
  description: "Comprehensive collection of Bible verses and powerful stories about reconciliation, forgiveness, restoration, peace, mercy, grace, and God's steadfast love. A spiritual resource for families seeking healing through God's Word.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} ${jetbrainsMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
